<?php
session_start();

if (!isset($_SESSION['usuario'])) {
    header("Location: login.php");
    exit();
}

include 'conexao.php';

$erro   = "";
$sucesso = "";

if (isset($_POST['cadastrar'])):
    $nome       = trim($_POST['nome'] ?? "");
    $quantidade = intval($_POST['quantidade'] ?? 0);

    if (empty($nome)) {
        $erro = "O nome do produto é obrigatório.";
    } elseif ($quantidade <= 0) {
        $erro = "A quantidade deve ser maior que zero.";
    } else {
        $nome_seg = mysqli_real_escape_string($conexao, $nome);
        $sql = mysqli_query($conexao,
            "INSERT INTO `objetos` (`nome`, `quantidade`) VALUES ('$nome_seg', '$quantidade')"
        );

        if ($sql) {
            $sucesso = "Produto \"$nome\" cadastrado com sucesso!";
        } else {
            $erro = "Erro ao cadastrar. Tente novamente.";
        }
    }
endif;
?>
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Cadastro de Objetos</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Sora:wght@300;400;500;600;700&display=swap');

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --bg:      #0d0d0d;
      --surface: #161616;
      --border:  #222;
      --accent:  #6366f1;
      --accent2: #a78bfa;
      --danger:  #ef4444;
      --success: #22c55e;
      --text:    #f0f0f0;
      --muted:   #555;
    }

    body {
      font-family: 'Sora', sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      background-image:
        radial-gradient(ellipse at 10% 0%,  rgba(99,102,241,.12) 0%, transparent 55%),
        radial-gradient(ellipse at 90% 90%, rgba(167,139,250,.08) 0%, transparent 50%);
    }

    /* ── TOPBAR ── */
    .topbar {
      position: fixed;
      top: 0; left: 0; right: 0;
      height: 60px;
      background: rgba(13,13,13,.9);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 32px;
      z-index: 100;
    }

    .topbar-logo {
      font-size: 13px;
      font-weight: 700;
      letter-spacing: 4px;
      text-transform: uppercase;
      color: var(--accent);
    }

    .btn-voltar {
      display: flex;
      align-items: center;
      gap: 7px;
      padding: 8px 16px;
      background: transparent;
      border: 1px solid var(--border);
      border-radius: 8px;
      color: #aaa;
      font-family: 'Sora', sans-serif;
      font-size: 13px;
      font-weight: 500;
      text-decoration: none;
      transition: border-color .2s, color .2s;
    }

    .btn-voltar:hover {
      border-color: var(--accent);
      color: var(--accent);
    }

    /* ── CONTEÚDO ── */
    .conteudo {
      max-width: 520px;
      margin: 0 auto;
      padding: 96px 24px 60px;
    }

    .page-header { margin-bottom: 32px; }

    .page-header h1 {
      font-size: 26px;
      font-weight: 700;
    }

    .page-header p {
      font-size: 13px;
      color: var(--muted);
      margin-top: 6px;
    }

    /* ── CARD DO FORMULÁRIO ── */
    .card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 40px 36px;
      box-shadow: 0 25px 60px rgba(0,0,0,.4);
    }

    label {
      display: block;
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 1px;
      text-transform: uppercase;
      color: #666;
      margin-bottom: 6px;
    }

    input {
      width: 100%;
      padding: 12px 14px;
      background: #1e1e1e;
      border: 1px solid #2e2e2e;
      border-radius: 8px;
      color: var(--text);
      font-family: 'Sora', sans-serif;
      font-size: 14px;
      margin-bottom: 20px;
      transition: border-color .2s;
      outline: none;
    }

    input:focus { border-color: var(--accent); }

    /* Remove setas do input number */
    input[type=number]::-webkit-inner-spin-button,
    input[type=number]::-webkit-outer-spin-button { -webkit-appearance: none; }
    input[type=number] { -moz-appearance: textfield; }

    .btn-cadastrar {
      width: 100%;
      padding: 13px;
      background: var(--accent);
      color: #fff;
      font-family: 'Sora', sans-serif;
      font-size: 14px;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background .2s, transform .1s;
      margin-bottom: 12px;
    }

    .btn-cadastrar:hover  { background: #4f52d9; }
    .btn-cadastrar:active { transform: scale(.98); }

    .btn-limpar {
      width: 100%;
      padding: 12px;
      background: transparent;
      border: 1px solid var(--border);
      border-radius: 8px;
      color: #aaa;
      font-family: 'Sora', sans-serif;
      font-size: 14px;
      font-weight: 500;
      cursor: pointer;
      transition: border-color .2s, color .2s;
    }

    .btn-limpar:hover {
      border-color: var(--accent);
      color: var(--accent);
    }

    /* ── MENSAGENS ── */
    .msg-erro {
      background: rgba(239,68,68,.12);
      border: 1px solid rgba(239,68,68,.3);
      color: #f87171;
      font-size: 13px;
      padding: 11px 14px;
      border-radius: 8px;
      margin-bottom: 20px;
    }

    .msg-sucesso {
      background: rgba(34,197,94,.12);
      border: 1px solid rgba(34,197,94,.3);
      color: #4ade80;
      font-size: 13px;
      padding: 11px 14px;
      border-radius: 8px;
      margin-bottom: 20px;
    }

    .divider { border: none; border-top: 1px solid var(--border); margin: 20px 0; }

    /* ── DICA ── */
    .dica {
      font-size: 11px;
      color: #444;
      margin-top: -14px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>

<header class="topbar">
  <span class="topbar-logo">&#9632; Sistema</span>
  <a href="menu.php" class="btn-voltar">
    ← Voltar ao menu
  </a>
</header>

<main class="conteudo">
  <div class="page-header">
    <h1>📦 Cadastro de Objetos</h1>
    <p>Informe o nome e a quantidade do produto.</p>
  </div>

  <div class="card">

    <?php if ($erro):    ?><div class="msg-erro">⚠️ <?= htmlspecialchars($erro) ?></div><?php endif; ?>
    <?php if ($sucesso): ?><div class="msg-sucesso">✅ <?= htmlspecialchars($sucesso) ?></div><?php endif; ?>

    <form method="POST" action="" autocomplete="off" id="form">

      <label for="nome">Nome do Produto</label>
      <input type="text" id="nome" name="nome"
             placeholder="Ex: Cadeira, Notebook, Caneta..."
             value="<?= htmlspecialchars($_POST['nome'] ?? '') ?>"
             required autofocus>

      <label for="quantidade">Quantidade</label>
      <input type="number" id="quantidade" name="quantidade"
             placeholder="Ex: 10"
             min="1"
             value="<?= htmlspecialchars($_POST['quantidade'] ?? '') ?>"
             required>
      <p class="dica">Informe somente números inteiros maiores que zero.</p>

      <button type="submit" name="cadastrar" class="btn-cadastrar">
        Cadastrar produto
      </button>

      <button type="button" class="btn-limpar" onclick="limparForm()">
        Limpar campos
      </button>

    </form>

  </div>
</main>

<script>
  function limparForm() {
    document.getElementById('nome').value = '';
    document.getElementById('quantidade').value = '';
    document.getElementById('nome').focus();
  }
</script>

</body>
</html>
